import {SidebarType} from "./state";

export type SideBarActionsTypes = any;

const sidebarReducer = (state: SidebarType, action: SideBarActionsTypes) => {
  return state;
}

export default sidebarReducer;